import numpy as np
import matplotlib.pyplot as plt

"""
Useful code snippets:
    - to create an empty list:: my_list = []
    - add entry to a list:: my_list.append(value)
    - generate random uniform number:: np.random.uniform(min_val, max_val, num_samples)
    
    - for loop for 10 times::
    for k in range(10):
        do_something
    
    - checking inequality:: x <= 10 
"""


def generate_random_throw(board_size):
    pass


def check_within_circle(x, y, radius):
    pass


if __name__ == "__main__":
    pass
